# embedded-animations

Visualizations of embedded systems architectures and paradigms using Motion Canvas.

## Setup

```bash
# install dependencies
npm install

# start the dev server (opens browser editor)
npm run dev
```

Then open http://localhost:9000 in your browser.
